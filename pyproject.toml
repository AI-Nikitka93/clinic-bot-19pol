[project]
name = "app"
version = "0.1.0"
dependencies = [ "sqlalchemy[asyncio]", "asyncpg", "alembic", "pydantic-settings", "redis", "aiogram", "fastapi", "uvicorn", "sqladmin", "itsdangerous", "jinja2" ]
requires-python = "~=3.12.0"
